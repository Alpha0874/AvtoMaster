<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Базовая тема (тёмная) -->
    <style name="Base.Theme.AvtoMaster" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/orange_accent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowBackground">@color/background_dark</item>
        <item name="colorPrimary">@color/orange_accent</item>
        <item name="colorPrimaryDark">@color/orange_accent_dark</item>
        <item name="colorAccent">@color/orange_accent</item>
    </style>

    <style name="Theme.AvtoMaster" parent="Base.Theme.AvtoMaster" />

    <!-- Светлая тема -->
    <style name="Theme.AvtoMaster.Light" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/orange_accent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowBackground">@color/background_light</item>
        <item name="colorPrimary">@color/orange_accent</item>
        <item name="colorPrimaryDark">@color/orange_accent_dark</item>
        <item name="colorAccent">@color/orange_accent</item>
    </style>
</resources>